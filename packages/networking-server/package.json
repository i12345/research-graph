{
  "name": "@graph/networking-server",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "pnpm run clean",
    "prebuild:debug": "pnpm run clean",
    "build": "tsc",
    "build:debug": "tsc -p tsconfig.debug.json",
    "test:lib": "pnpm run build:debug && pnpm run test",
    "keygen": "chmod +x ./scripts/keygen.sh && ./scripts/keygen.sh",
    "pretest": "pnpm run keygen",
    "test": "ava"
  },
  "dependencies": {
    "@graph/utils": "workspace:",
    "@graph/networking": "workspace:",
    "socket.io": "catalog:",
    "bufferutil": "catalog:",
    "@fails-components/webtransport": "catalog:",
    "@fails-components/webtransport-transport-http3-quiche": "catalog:"
  },
  "devDependencies": {
    "get-port": "catalog:",
    "utf-8-validate": "catalog:",
    "@ava/typescript": "catalog:",
    "@graph/tsconfig": "workspace:",
    "ava": "catalog:",
    "typescript": "catalog:"
  },
  "ava": {
    "files": [
      "src/**/*.spec.ts"
    ],
    "typescript": {
      "rewritePaths": {
        "src/": "dist/"
      },
      "extensions": [
        "ts"
      ],
      "compile": false
    }
  },
  "license": "See LICENSE.md"
}
